# Calybrix

Sitio estático (HTML). Página principal: `index.html` (copia de `Calybrix_pitch_updated.html`).

## Desarrollo local

Abrir `index.html` en el navegador, o servir con:

```bash
python3 -m http.server 8000
```
